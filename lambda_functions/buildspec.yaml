version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.x
  build:
    commands:
      - echo "Packaging Lambda functions"
      - cd lambda_functions
      - zip -r lambda_deploy.zip .
artifacts:
  files:
    - lambda_functions/lambda_deploy.zip